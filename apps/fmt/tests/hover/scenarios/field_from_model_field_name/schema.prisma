generator js {
    provider = "prisma-client"
}

datasource db {
    provider = "postgres"
    url      = env("DATABASE_URL")
}

model ModelNameA {
    id String @id @map("_id")

    /// very important documentation
    veryImpor<|>tantField DateTime
}
