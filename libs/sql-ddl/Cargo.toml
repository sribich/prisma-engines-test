[package]
name = "sql-ddl"
version = "0.1.0"
edition.workspace = true

[features]
postgres = []
mysql = []
sqlite = []
default = ["postgres", "mysql", "sqlite"]

[dependencies]

[dev-dependencies]
indoc.workspace = true
